{
  "name": "monosense",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve -p 3000 .",
    "build": "mkdir -p dist && cp -r *.html *.js *.png dist/"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  }
}
